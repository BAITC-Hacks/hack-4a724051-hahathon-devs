[project]
name = "ekt-assistant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=1.8",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
