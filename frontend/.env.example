# Server-side only. No API keys belong in the frontend.
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
# Optional exact public origin when running behind a trusted reverse proxy.
# FRONTEND_ORIGIN=https://your-marketplace.example
