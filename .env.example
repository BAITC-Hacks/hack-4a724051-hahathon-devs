# Copy to .env locally. Never commit real credentials.
# These secrets are backend-only; never use VITE_ or NEXT_PUBLIC_ prefixes.
EKT_API_USER=
EKT_API_PASSWORD=
LLM_API_KEY=
# Assistant: without LLM_ENABLED=true the model is never called
LLM_ENABLED=false
LLM_MODEL=claude-opus-5
LLM_EFFORT=low
# Current HTTP runtime keeps paid calls disabled until budgets are integrated.
# Set synthetic explicitly for the local demo catalog and demo cart.
INTEGRATION_MODE=unavailable
# Catalog DB (participant 1). catalog_db reads products from PostgreSQL:
#   python -m app.catalog_cli migrate && python -m app.catalog_cli import-synthetic
DATABASE_URL=
# Re-read price and stock from the EKT API before cart actions (needs EKT_API_* above)
EKT_LIVE_REFRESH=false
